import React from 'react';
import { Button } from 'react-bootstrap';

const defaultBtn = <Button>Default Button</Button>;

const primaryBtn = <Button bsStyle="primary">Primary Button</Button>;

const secondaryBtn = <Button bsStyle="secondary">Secondary Button</Button>;

export { defaultBtn, primaryBtn, secondaryBtn };
