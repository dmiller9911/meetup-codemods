import React from 'react';
import Button from '@material-ui/core/Button';

const defaultBtn = <Button>Default Button</Button>;

const primaryBtn = <Button variant="primary">Primary Button</Button>;

const secondaryBtn = <Button variant="secondary">Secondary Button</Button>;

export { defaultBtn, primaryBtn, secondaryBtn };
